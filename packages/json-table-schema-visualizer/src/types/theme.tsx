export interface ThemeConfigValue {
  text: {
    900: string;
    700: string;
  };
  connection: {
    default: string;
    active: string;
  };
  tableHeader: {
    bg: string;
    fg: string;
  };
  colAccent: string;
  table: {
    bg: string;
    shadow: string;
  };
}
